[blank_image]
recipe = executable_args
error_tolerance = False
nodescript = python_plugin
executable = %(factorroot)s/scripts/blank_image.py
max_per_node = %(max_proc_per_node)s

[calculate_bl_lengths]
recipe = executable_args
error_tolerance = False
nodescript = python_plugin
executable = %(factorroot)s/scripts/calculate_bl_lengths.py
max_per_node = %(max_proc_per_node)s

[collect_h5parms]
recipe = executable_args
error_tolerance = False
executable = %(h5collector_executable)s
max_per_node = %(max_proc_per_node)s

[combine_h5parms]
recipe = executable_args
error_tolerance = False
nodescript = python_plugin
executable = %(factorroot)s/scripts/combine_h5parms.py
max_per_node = %(max_proc_per_node)s

[combine_skymodels]
recipe = executable_args
error_tolerance = False
nodescript = python_plugin
executable = %(factorroot)s/scripts/combine_skymodels.py
max_per_node = %(max_proc_per_node)s

[dppp]
recipe = executable_args
error_tolerance = False
parsetasfile = False
executable = %(lofarroot)s/bin/DPPP
outputsuffixes = []
args_format = lofar
outputkey = msout
max_per_node = %(max_proc_per_node)s

[dppp_single]
recipe = executable_args
error_tolerance = False
parsetasfile = False
executable = %(lofarroot)s/bin/DPPP
outputsuffixes = []
args_format = lofar
outputkey = msout
max_per_node = 1

[dppp_inplace]
recipe = executable_args
error_tolerance = False
parsetasfile = False
executable = %(lofarroot)s/bin/DPPP
args_format = lofar
inplace = True
max_per_node = %(max_proc_per_node)s

[filter_skymodel]
recipe = executable_args
error_tolerance = False
nodescript = python_plugin
executable = %(factorroot)s/scripts/filter_skymodel.py
max_per_node = %(max_proc_per_node)s

[fit_fast_phase_screens]
recipe = executable_args
error_tolerance = False
nodescript = python_plugin
inplace = True
executable = %(factorroot)s/scripts/fit_fast_phase_screens.py
max_per_node = %(max_proc_per_node)s

[fit_gain_screens]
recipe = executable_args
error_tolerance = False
nodescript = python_plugin
inplace = True
executable = %(factorroot)s/scripts/fit_gain_screens.py
max_per_node = %(max_proc_per_node)s

[fit_tec_screens]
recipe = executable_args
error_tolerance = False
nodescript = python_plugin
inplace = True
executable = %(factorroot)s/scripts/fit_tec_screens.py
max_per_node = %(max_proc_per_node)s

[flag_residual_gains]
recipe = executable_args
error_tolerance = False
nodescript = python_plugin
inplace = True
executable = %(factorroot)s/scripts/flag_residual_gains.py
max_per_node = %(max_proc_per_node)s

[make_aterm_config]
recipe = executable_args
error_tolerance = False
nodescript = python_plugin
executable = %(factorroot)s/scripts/make_aterm_config.py
max_per_node = %(max_proc_per_node)s

[make_aterm_images]
recipe = executable_args
error_tolerance = False
nodescript = python_plugin
executable = %(factorroot)s/scripts/make_aterm_images.py
max_per_node = %(max_proc_per_node)s

[make_clean_mask]
recipe = executable_args
error_tolerance = False
nodescript = python_plugin
executable = %(factorroot)s/scripts/make_clean_mask.py
max_per_node = %(max_proc_per_node)s

[make_mosaic]
recipe = executable_args
error_tolerance = False
nodescript = python_plugin
executable = %(factorroot)s/scripts/make_mosaic.py
max_per_node = %(max_proc_per_node)s

[make_mosaic_template]
recipe = executable_args
error_tolerance = False
nodescript = python_plugin
executable = %(factorroot)s/scripts/make_mosaic_template.py
max_per_node = %(max_proc_per_node)s

[make_selfcal_plots]
recipe = executable_args
error_tolerance = False
nodescript = python_plugin
executable = %(factorroot)s/scripts/plot_selfcal_solutions.py
max_per_node = %(max_proc_per_node)s

[make_sourcedb]
recipe = executable_args
executable = %(lofarroot)s/bin/makesourcedb
args_format = lofar
outputkey = out
max_per_node = %(max_proc_per_node)s

[merge_skymodels]
recipe = executable_args
error_tolerance = False
nodescript = python_plugin
executable = %(factorroot)s/scripts/merge_skymodels.py
max_per_node = %(max_proc_per_node)s

[mosaic_images]
recipe = executable_args
error_tolerance = False
nodescript = python_plugin
executable = %(factorroot)s/scripts/mosaic_images.py
max_per_node = %(max_proc_per_node)s

[pad_image]
recipe = executable_args
nodescript = python_plugin
executable = %(factorroot)s/scripts/pad_image.py
max_per_node = %(max_proc_per_node)s

[pb_correct]
recipe = executable_args
error_tolerance = False
nodescript = python_plugin
executable = %(factorroot)s/scripts/pb_correct.py
outputsuffixes = [.pbcor.fits,.pbcut.fits]
max_per_node = %(max_proc_per_node)s

[pre_average]
recipe = executable_args
error_tolerance = False
nodescript = python_plugin
executable = %(factorroot)s/scripts/pre_average_time.py
max_per_node = %(max_proc_per_node)s

[pre_average_freq]
recipe = executable_args
error_tolerance = False
nodescript = python_plugin
executable = %(factorroot)s/scripts/pre_average_freq.py
max_per_node = %(max_proc_per_node)s

[regrid_image]
recipe = executable_args
error_tolerance = False
nodescript = python_plugin
executable = %(factorroot)s/scripts/regrid_image.py
max_per_node = 2

[remove_file]
recipe = executable_args
error_tolerance = False
executable = /bin/rm
inplace = True
max_per_node = %(max_proc_per_node)s

[reset_h5parm]
recipe = executable_args
error_tolerance = False
nodescript = python_plugin
executable = %(factorroot)s/scripts/reset_h5parm.py
max_per_node = %(max_proc_per_node)s
inplace = True

[reweight]
recipe = executable_args
error_tolerance = False
nodescript = python_plugin
executable = %(factorroot)s/scripts/reweight.py
max_per_node = 2

[smooth_amps_spline]
recipe = executable_args
error_tolerance = False
nodescript = python_plugin
executable = %(factorroot)s/scripts/smooth_amps_spline.py
max_per_node = %(max_proc_per_node)s

[subtract_sector_models]
recipe = executable_args
error_tolerance = False
nodescript = python_plugin
executable = %(factorroot)s/scripts/subtract_sector_models.py
inplace = True
max_per_node = 1

[wsclean]
recipe = executable_args
error_tolerance = False
outputkey = name
parsetasfile = False
executable = %(wsclean_executable)s
outputsuffixes = [-MFS-I-image.fits,-MFS-I-image-pb.fits,-MFS-Q-image.fits,-MFS-Q-image-pb.fits,-MFS-U-image.fits,-MFS-U-image-pb.fits,-MFS-V-image.fits,-MFS-V-image-pb.fits,-MFS-image.fits,-MFS-image-pb.fits,-MFS-model.fits,-MFS-model-pb.fits,-MFS-residual.fits,-sources.txt,-sources-pb.txt]
max_per_node = %(max_proc_per_node)s
args_format = wsclean

[wsclean_ft]
recipe = executable_args
error_tolerance = False
parsetasfile = False
executable = %(wsclean_executable)s
max_per_node = %(max_proc_per_node)s
inplace = True
args_format = wsclean
